#!/usr/bin/env bash

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Go program with all passed arguments
go run "$SCRIPT_DIR/go/jwtdec/main.go" "$@"
